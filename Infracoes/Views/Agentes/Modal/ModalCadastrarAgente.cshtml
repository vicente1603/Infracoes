<div class="modal fade" data-app-modal="agentes.modal.dados" data-backdrop="static">
    <div class="modal-dialog">
        <div class="modal-content">
            <form name="usuarios.form.dados">
                <div class="modal-header">
                    <button type="button" class="close" data-app-modal-dismiss="agentes.modal.dados"><span>&times;</span></button>
                    <h4 class="modal-title">Novo Agente</h4>
                </div>
                <div class="modal-body">
                    <div class="alert alert-danger" data-ng-show="agentes.modal.erros.aoSalvar">
                        <button type="button" class="close" data-ng-click="agentes.modal.limparMensagens()"><span aria-hidden="true">&times;</span></button>
                        <span data-ng-repeat="erro in agentes.modal.erros.aoSalvar">
                            <span data-ng-bind="erro"></span><br />
                        </span>
                    </div>
                    <div class="row">
                        <div class="col-md-12">                            
                            <div class="form-group">
                                <label>Nome</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="agentes.agente.nomeAgente"
                                       required />
                                <label>Matricula</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="agentes.agente.matricula"
                                       required />
                                <label>Efetivação</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="agentes.agente.efetivacao"
                                       data-app-calendario
                                       required />
                                <label>Tempo de Serviço</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="agentes.agente.tempoServico"
                                       required />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-success"
                            data-ng-disabled="agentes.form.dados.$invalid || agentes.modal.salvando"
                            data-ng-click="agentes.modal.salvar()">
                        <span data-ng-show="agentes.modal.salvando">Salvando&nbsp;<i class="fa fa-refresh fa-spin"></i></span>
                        <span data-ng-hide="agentes.modal.salvando">Salvar</span>
                    </button>
                    <span>&nbsp;ou</span>
                    <button type="button" class="btn btn-link" data-app-modal-dismiss="agentes.modal.dados">
                        Fechar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>