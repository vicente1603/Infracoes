<div class="modal fade" data-app-modal="agentes.modal.confirmarRemover" data-backdrop="static">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-app-modal-dismiss="agentes.modal.confirmarRemover"><span>&times;</span></button>
                Remover Agente
            </div>
            <div class="modal-body">
                <div class="alert alert-danger" data-ng-show="agentes.modal.erros.aoRemover">
                    <button type="button" class="close" data-ng-click="agentes.modal.limparMensagens()"><span aria-hidden="true">&times;</span></button>
                    <span data-ng-repeat="erro in agentes.modal.erros.aoRemover" data-ng-bind="erro"></span>
                </div>
                <p>
                    Deseja realmente remover o agente <strong data-ng-bind="agentes.agente.matricula"></strong> ?
                </p>
            </div>
            <div class="modal-footer">
                <button type="button"
                        class="btn btn-success"
                        data-ng-click="agentes.modal.remover()">
                    <span data-ng-show="agentes.modal.removendo"
                          data-ng-disabled="agentes.modal.removendo">
                        Removendo&nbsp;<i class="fa fa-refresh fa-spin"></i>
                    </span>
                    <span data-ng-hide="agentes.modal.removendo">Sim</span>
                </button>
                <button type="button"
                        class="btn btn-danger"
                        data-app-modal-dismiss="agentes.modal.confirmarRemover">
                    Não
                </button>
            </div>
        </div>
    </div>
</div>


