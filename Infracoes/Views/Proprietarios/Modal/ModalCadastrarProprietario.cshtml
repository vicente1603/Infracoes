<div class="modal fade" data-app-modal="proprietarios.modal.dados" data-backdrop="static">
    <div class="modal-dialog">
        <div class="modal-content">
            <form name="usuarios.form.dados">
                <div class="modal-header">
                    <button type="button" class="close" data-app-modal-dismiss="proprietarios.modal.dados"><span>&times;</span></button>
                    <h4 class="modal-title">Novo Proprietário</h4>
                </div>
                <div class="modal-body">
                    <div class="alert alert-danger" data-ng-show="proprietarios.modal.erros.aoSalvar">
                        <button type="button" class="close" data-ng-click="proprietarios.modal.limparMensagens()"><span aria-hidden="true">&times;</span></button>
                        <span data-ng-repeat="erro in proprietarios.modal.erros.aoSalvar">
                            <span data-ng-bind="erro"></span><br />
                        </span>
                    </div>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label>Nome</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="proprietarios.proprietario.nomeProprietario"
                                       required />
                            </div>
                            <div class="form-group">
                                <label>Cpf</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="proprietarios.proprietario.cpfProprietario"
                                       required />
                            </div>
                            <div class="form-group">
                                <label>Data de Nascimento</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="proprietarios.proprietario.dataNascimento"
                                       data-app-calendario
                                       required />
                            </div>
                            <div class="form-group">
                                <label>Telefone</label>
                                <input class="form-control"
                                       maxlength="50"
                                       data-ng-model="proprietarios.proprietario.telefone"
                                       required />
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button"
                            class="btn btn-success"
                            data-ng-disabled="proprietarios.form.dados.$invalid || proprietarios.modal.salvando"
                            data-ng-click="proprietarios.modal.salvar()">
                        <span data-ng-show="proprietarios.modal.salvando">Salvando&nbsp;<i class="fa fa-refresh fa-spin"></i></span>
                        <span data-ng-hide="proprietarios.modal.salvando">Salvar</span>
                    </button>
                    <span>&nbsp;ou</span>
                    <button type="button" class="btn btn-link" data-app-modal-dismiss="proprietarios.modal.dados">
                        Fechar
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

